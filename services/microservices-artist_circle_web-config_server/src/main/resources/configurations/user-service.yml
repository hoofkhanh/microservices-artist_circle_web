spring:
  datasource:
    url: jdbc:postgresql://localhost:5430/user
    username: hoofkhanh
    password: hoofkhanh
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
  flyway:
    baseline-on-migrate: true
    enabled: true
    baseline-description: "init"
    baseline-version: 0
    user: ${spring.datasource.username}
    password: ${spring.datasource.password}
  
  data:  
    redis:
      host: localhost
      port: 6379
      database: 0
      
  mail:
    host: localhost
    port: 1025
    username: hoofkhanh@gmail.com
    password: hoofkhanh
    properties:
      mail:
        smtp:
          trust: "*"
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000