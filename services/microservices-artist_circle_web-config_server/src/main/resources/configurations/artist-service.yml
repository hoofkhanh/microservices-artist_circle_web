spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5430}/${DB_NAME:artist}
    username: ${DB_USERNAME:hoofkhanh}
    password: ${DB_PASSWORD:hoofkhanh}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
  flyway:
    baseline-on-migrate: true
    enabled: true
    baseline-description: "init"
    baseline-version: 0
    user: ${spring.datasource.username}
    password: ${spring.datasource.password}

  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5671}
    virtual-host: ${RABBITMQ_VHOST:/} #này giống như tên database vậy
    
server:
  port: ${SERVER_PORT:8081}